---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{.Values.eventstore.pvc.name}}
  namespace: {{.Values.general.namespace}}
spec:
  accessModes:
    - ReadWriteMany
  storageClassName:
  resources:
    requests:
      storage: {{.Values.eventstore.pvc.storage}}
...
